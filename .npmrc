engine-strict=true

# npm 12+ defaults to blocking git-protocol dependencies (allow-git=none).
# @davidsneighbour/remark-config depends on remark-lint-frontmatter-schema
# via a git+ssh URL to another davidsneighbour/* repo; both are trusted,
# first-party sources, so allow git installs for this project.
allow-git=all
