---
import FeedReader from "@components/content/FeedReader.astro";
import Featured from "@components/sections/Featured.astro";
import Recent from "@components/sections/Recent.astro";
import setup from "@data/setup.json" with { type: "json" };
import Layout from "@layouts/DefaultPage.astro";

const post = {
  data: {
    description: setup.description,
    title: "Welcome on " + setup.title,
  },
};
---

<Layout post={post}>
  <Featured />
  <Recent />
  <FeedReader />
</Layout>
