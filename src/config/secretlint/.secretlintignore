scratch

# skills-lock.json stores SHA-256 hex digests (computedHash) which false-positive
# against the secp256k1 private key detector.
skills-lock.json

# src/data/agent-skills.json stores SHA-256 content digests for the Agent
# Skills Discovery index, same false-positive as skills-lock.json above.
src/data/agent-skills.json
