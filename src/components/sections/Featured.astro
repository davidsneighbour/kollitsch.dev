---
import { getHomepagePosts } from '@utils/content';
import BlogPost from '@components/BlogPost.astro';

const { featuredPost, recentPosts: _ } = await getHomepagePosts();
---

<BlogPost post={featuredPost!} />
