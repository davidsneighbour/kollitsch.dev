{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.davidsneighbour.dev/schemas/webcomponents.schema.json",
  "title": "Web Components Configuration",
  "description": "Defines a list of web components that can be auto-detected by the remark plugin.",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string"
    },
    "components": {
      "type": "array",
      "title": "Web Components List",
      "description": "List of web components available for detection and conditional import.",
      "items": {
        "type": "object",
        "title": "Web Component Definition",
        "properties": {
          "slug": {
            "type": "string",
            "pattern": "^[a-z0-9-]+$",
            "description": "Unique tag slug for the web component (used as <slug> in HTML)."
          },
          "import": {
            "type": "string",
            "description": "ESM import path for the web component (e.g., '@components/MyComponent.astro')."
          },
          "enabled": {
            "type": "boolean",
            "default": true,
            "description": "Whether the web component should be detected (default: true)."
          },
          "description": {
            "type": "string",
            "description": "Optional human-readable description of the web component."
          }
        },
        "required": [
          "slug",
          "import"
        ],
        "additionalProperties": false
      },
      "minItems": 0
    }
  },
  "required": [
    "components"
  ],
  "additionalProperties": false
}
