# Animation plans

Generated by the `improve-animations` audit skill, scoped to the site title header (`src/components/layout/header/title/SiteTitle.astro`) and its sticky counterpart (`#navbar-brand` in `src/components/layout/header/Header.astro`). Commit at time of audit: `28fb96812a30`.

## Recommended execution order

1. **[001—fix title morph restart](001-fix-title-morph-restart.md)** (HIGH)—do this first. It rewrites the `color` transition on `.text-image-fill` from `@keyframes` to a plain `transition`, which changes the exact declaration plans 003 and 004 also touch.
2. **[003—unify hover reveal easing](003-unify-hover-reveal-easing.md)** (MEDIUM)—depends on 001 having landed first if you want the color transition's curve updated too; the plan notes how to adapt if 001 hasn't landed yet.
3. **[004—fix reduced-motion opacity/color](004-fix-reduced-motion-opacity-color.md)** (MEDIUM)—depends on 001 and 003 for matching the `color` transition's duration/curve in the reduced-motion fallback; the plan notes how to adapt if applied out of order.
4. **[002—fix navbar-brand transition-all](002-fix-navbar-brand-transition-all.md)** (HIGH)—fully independent, touches only `Header.astro`. Can be done in any order relative to the other three.
5. **[005—fix clip-path curve mismatch](005-fix-clip-path-curve-mismatch.md)** (MEDIUM)—found on a follow-up re-audit after 001-004 landed. Independent of the others, touches only `SiteTitle.astro`.

## Status

| Plan | Severity | Status |
| --- | --- | --- |
| 001—fix title morph restart | HIGH | Done (`ed2b0dc3e00f`) |
| 002—fix navbar-brand transition-all | HIGH | Done (`a6899c58b82f`) |
| 003—unify hover reveal easing | MEDIUM | Done (`781911fd9db0`) |
| 004—fix reduced-motion opacity/color | MEDIUM | Done (`ff4be676e5c5`) |
| 005—fix clip-path curve mismatch | MEDIUM | Done (`1c69d3227c9e`) |

## Not turned into a plan

- **Missed opportunity:** `.site-title-link` has an elaborate hover choreography but no `:active`/press feedback (`scale(0.97)` on press). Additive, not corrective—raise it separately if wanted.
- **Confirmed correct, no plan needed:** `.site-title-hero`'s scroll-linked exit animation (`animation-timeline: view()`) correctly uses `linear` easing—scroll-scrubbed animations should stay linear since the scroll gesture itself supplies the curve.

## Re-audit history

- A follow-up run of the `improve-animations` skill after 001-004 landed found finding 005 (the clip-path/curve mismatch above) and confirmed all four original findings were correctly resolved with no regressions.
