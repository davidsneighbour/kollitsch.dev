# E2E test coverage TODO

The following high-value scenarios are not covered in `src/test` yet:

- Contact page submission flow (`/connect/`), including success and validation error states.
- Search integration smoke test (`/search/` or site search UI), including keyboard accessibility.
- Blog post template smoke test against one real post route (metadata, heading hierarchy, and canonical link).
- 404 route verification (`/404/` rendering and navigation options).
